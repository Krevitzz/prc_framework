# PLAN MIGRATION PROFILING - ORDRE OPTIMAL

## PHASE 1 : TEMPLATES JINJA2 (avec gamma_profiling actuel)
**Objectif** : Finaliser système reports réutilisable

### 1.1 Création templates
- [✅] `summary_global.md.j2` (synthèse multi-axes)
- [✅] `summary_axis.md.j2` (générique tous axes)
- [✅ `profile_entity.json.j2` (profil individuel)

### 1.2 Intégration report_writers
- [ ] Installer `jinja2` (si absent)
- [ ] Implémenter `render_template(template_name, context)`
- [ ] Adapter `write_summary_global()` → Jinja2
- [ ] Adapter `write_summary_axis()` → Jinja2
- [ ] Garder helpers existants (write_header, write_key_value, etc.)

### 1.3 Validation avec gamma_profiling actuel
- [ ] Exécuter pipeline complet (gamma uniquement)
- [ ] Générer rapports Jinja2
- [ ] Comparer avec rapports actuels (manuellement)
- [ ] Valider lisibilité, exhaustivité

**✅ LIVRABLE** : Système reports Jinja2 fonctionnel avec gamma_profiling

---

## PHASE 2 : VALIDATION PROFILING_COMMON (parallèle)
**Objectif** : Garantir équivalence gamma_profiling ↔ profiling_common

### 2.1 Tests rétrocompatibilité
- [ ] Exécuter ancien pipeline → `old_gamma_results.json`
- [ ] Exécuter nouveau pipeline (`profiling_common.profile_all_gammas()`) → `new_gamma_results.json`
- [ ] Script comparaison JSON (deep diff)
- [ ] Tolérance float (1e-6)
- [ ] Validation structure identique

### 2.2 Tests ranking
- [ ] Comparer `gamma_profiling.rank_gammas_by_test()`
- [ ] Avec `cross_profiling.rank_entities_by_metric(..., 'test', 'SYM-001')`
- [ ] Vérifier ordre identique
- [ ] Vérifier scores identiques

**✅ LIVRABLE** : Preuve équivalence mathématique profiling_common

---

## PHASE 3 : MIGRATION VERDICT_REPORTER (switch atomique)
**Objectif** : Basculer vers profiling_common sans casser rapports

### 3.1 Refactor imports verdict_reporter
```python
# Avant
from ..utils.gamma_profiling import (
    profile_all_gammas,
    compare_gammas_summary
)

# Après
from ..utils.profiling_common import (
    profile_all_gammas,  # Même signature !
    compare_gammas_summary
)
```

### 3.2 Validation switch
- [ ] Exécuter pipeline complet
- [ ] Comparer rapports avant/après (Jinja2 identiques)
- [ ] Vérifier aucune régression

**✅ LIVRABLE** : verdict_reporter utilise profiling_common

---

## PHASE 4 : EXTENSION NOUVEAUX AXES (incrémental)
**Objectif** : Profiling modifier, encoding, test

### 4.1 Profiling modifier
```python
# Dans verdict_reporter
modifier_profiles = profiling_common.profile_all_modifiers(observations)
modifier_summary = profiling_common.compare_modifiers_summary(modifier_profiles)
```
- [ ] Exécuter profiling modifier
- [ ] Générer `summary_modifier.txt` (Jinja2)
- [ ] Validation manuelle résultats

### 4.2 Profiling encoding
- [ ] Idem modifier (via `profile_all_encodings()`)
- [ ] Générer `summary_encoding.txt`

### 4.3 Profiling test
- [ ] Idem test (via `profile_all_tests()`)
- [ ] Calculer `discriminant_power` (cross_profiling)
- [ ] Générer `summary_test.txt`

**✅ LIVRABLE** : 4 axes profiling opérationnels

---

## PHASE 5 : PROFILING_RUNNER (orchestration finale)
**Objectif** : Unification appels verdict_reporter

### 5.1 Intégration profiling_runner
```python
# Dans verdict_reporter
all_profiling = profiling_runner.run_all_profiling(
    observations,
    axes=['test', 'gamma', 'modifier', 'encoding']
)

gamma_profiles = all_profiling['gamma']['profiles']
modifier_profiles = all_profiling['modifier']['profiles']
# etc.
```

### 5.2 Génération rapports unifiés
- [ ] `summary_global.txt` (synthèse 4 axes)
- [ ] 4 × `summary_{axis}.txt`
- [ ] Profils individuels JSON
- [ ] Comparisons/rankings

**✅ LIVRABLE** : Pipeline complet 4 axes avec rapports unifiés

---

## PHASE 6 : NETTOYAGE (après validation complète)
**Objectif** : Supprimer gamma_profiling legacy

### 6.1 Suppression fichiers obsolètes
- [ ] Supprimer `tests/utilities/utils/gamma_profiling.py`
- [ ] Supprimer anciens catalogues si remplacés
- [ ] Vérifier aucun import cassé (grep récursif)

### 6.2 Documentation finale
- [ ] Mettre à jour `FUNCTIONS_INDEX.md`
- [ ] Mettre à jour catalogues (HUB, UTIL, PROFILING)
- [ ] Documenter migration dans CHANGELOG (si créé)

**✅ LIVRABLE** : Code propre, gamma_profiling supprimé

---

## CRITÈRES VALIDATION CHAQUE PHASE

### Phase 1 (Templates)
- ✅ Rapports Jinja2 générés
- ✅ Lisibilité validée manuellement
- ✅ Pas de régression vs rapports actuels

### Phase 2 (Validation)
- ✅ JSON diff clean (structure + valeurs)
- ✅ Rankings identiques
- ✅ Aucune erreur runtime

### Phase 3 (Migration)
- ✅ Rapports identiques avant/après switch
- ✅ Aucun import cassé
- ✅ Pipeline stable

### Phase 4 (Nouveaux axes)
- ✅ Format retour conforme Charter R5.1-A
- ✅ Rapports exploitables
- ✅ Insights nouveaux validés

### Phase 5 (Orchestration)
- ✅ `summary_global.txt` cohérent
- ✅ 4 axes synchronisés
- ✅ Navigation rapports fluide

### Phase 6 (Nettoyage)
- ✅ Aucun fichier mort
- ✅ Documentation à jour
- ✅ Code audit clean

---

## DÉPENDANCES CRITIQUES

```
Phase 1 (Templates)
    ↓
Phase 2 (Validation) ← doit passer AVANT Phase 3
    ↓
Phase 3 (Migration) ← switch atomique
    ↓
Phase 4 (Nouveaux axes) ← incrémental
    ↓
Phase 5 (Orchestration) ← unification
    ↓
Phase 6 (Nettoyage) ← après validation complète
```

**⚠️ RÈGLE ABSOLUE** : Aucune suppression gamma_profiling avant Phase 6

---

## ESTIMATION TEMPS

| Phase | Durée | Validation |
|-------|-------|------------|
| Phase 1 (Templates) | 3-4h | Tests manuels rapports |
| Phase 2 (Validation) | 2-3h | Script comparaison JSON |
| Phase 3 (Migration) | 1h | Tests regression |
| Phase 4 (Nouveaux axes) | 4-5h | Validation manuelle chaque axe |
| Phase 5 (Orchestration) | 2-3h | Tests intégration |
| Phase 6 (Nettoyage) | 1h | Audit code |

**TOTAL** : ~13-19h (2-3 jours de travail concentré)

---

## ROLLBACK STRATEGY

Chaque phase maintient possibilité retour arrière :

### Phase 1-2 : gamma_profiling intact
- Aucune modification pipeline existant
- Templates additionnels (pas remplacement)

### Phase 3 : Switch réversible
```python
# Rollback immédiat possible
from ..utils.gamma_profiling import ...  # Restaurer import
```

### Phase 4-5 : Nouveaux axes indépendants
- Gamma reste fonctionnel
- Échec nouveau axe → désactiver axe, garder gamma

### Phase 6 : Suppression après validation totale
- Backup gamma_profiling avant suppression
- Période observation 1 semaine

---

## NEXT STEP IMMÉDIAT

**ACTION** : Créer templates Jinja2 (Phase 1.1)

**Fichiers à créer** :
1. `tests/utilities/report_templates/summary_global.md.j2`
2. `tests/utilities/report_templates/summary_axis.md.j2`
3. `tests/utilities/report_templates/profile_entity.json.j2`

**Structure template minimal** (summary_axis.md.j2) :
```jinja2
# RAPPORT {{ axis|upper }} - PROFILING R0

Entités analysées : {{ n_entities }}
Tests profilés : {{ n_tests }}

## RÉGIMES DOMINANTS

{% for regime, entities in by_regime.items() %}
{{ regime }} : {{ entities|length }} entités
{% endfor %}

## RANKINGS PAR TEST

{% for test_name, ranking in by_test.items() %}
### {{ test_name }}
  Meilleur : {{ ranking.best_conservation }}
  Pire : {{ ranking.worst_conservation }}
{% endfor %}

{% if discriminant_powers %}
## POUVOIR DISCRIMINANT (tests uniquement)
{% for test, power in discriminant_powers.items() %}
{{ test }} : ratio {{ power.discriminant_ratio|round(2) }}
{% endfor %}
{% endif %}
```