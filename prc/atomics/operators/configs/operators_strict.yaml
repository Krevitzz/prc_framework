# configs/atomics/operators/strict.yaml
# Override default avec paramètres plus restrictifs

GAM-001:
  beta: 3.0  # Saturation plus forte

GAM-002:
  alpha: 0.02  # Diffusion plus lente

GAM-006:
  alpha: 0.1  # Inertie réduite

GAM-009:
  sigma: 0.005  # Bruit réduit

GAM-010:
  sigma: 0.01   # Bruit multiplicatif réduit
