# configs/constraints/pool_requirements.yaml
# Contraintes pool actif — verdict filtre selon ces règles

version: "1.0"

# Date dernière modification
last_updated: "2026-02-20"

# Contraintes globales (null = pas de contrainte)
n_dof:
  min: null  # Ex futur: 100 (après calibration R0-R2)
  max: null

max_iterations:
  min: null
  max: null  # Ex futur: 200 (après stabilité validée)

# Atomics deprecated (auto-skip dans verdict)
deprecated:
  gammas: []      # Ex futur: ['GAM-003'] si explosion systématique
  encodings: []   # Ex futur: ['SYM-008'] si pathologique
  modifiers: []

# Notes
# - Contraintes déterminées progressivement via phases R0, R1, R2, ...
# - Verdict filtre automatiquement selon ces règles
# - Parquets anciens restent intacts (traçabilité)
# - Concordance inter-phases = subset commun validant requirements
