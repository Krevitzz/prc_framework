# tests/config/global/verdict_strict_v1.yaml
# Mêmes sections, seuils plus stricts
normalization:
  default_method: "robust"
  scope: "per_test"
  outlier_threshold: 2.5  # Plus strict

patterns:
  non_discriminant:
    normalized_threshold: -1.5  # vs -2.0
    ...