phase: poc_v7_perf

n_dof          : [10, 20, 50]       # 3 shapes → 3 compilations/gamma
max_iterations : [100, 500]         # coût scan réel
seed_CI        : [0, 1, 2, 3]       # 4 seeds CI
seed_run       : [0, 1, 2, 3]       # 4 seeds run → 16 samples/groupe

axes:
  gamma:
    - id: GAM-001   # markovian, diff, simple
    - id: GAM-004   # markovian, diff, decay → cherche EXPLOSION
    - id: GAM-009   # stochastic, non-diff
    - id: GAM-005   # non_markovian, diff → carry étendu
  encoding:
    - id: SYM-002   # stochastique → seed_CI utile
    - id: SYM-005   # déterministe banded
  modifier:
    - id: M0