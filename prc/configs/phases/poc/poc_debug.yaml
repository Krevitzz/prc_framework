# configs/phases/poc/poc_debug.yaml
# POC debug — très court pour traçage

phase: poc_debug
max_iterations: 100  # Très court
n_dof: 100           # Petite dimension

axes:
  gamma:
    - [GAM-001]  # Ligne 1
    - [GAM-002]  # Ligne 2
  encoding:
    - id: SYM-001
  modifier:
    - id: M0
