# tests/config/global/thresholds_strict_v1.yaml
# Seuils verdicts R0 - Configuration stricte
# Architecture Charter 5.4 - Section 12.9

version: "1.0"
config_id: "thresholds_strict_v1"
description: "Seuils verdicts R0 - configuration stricte (exploration ciblée)"
created_date: "2025-01-02"
charter_reference: "Section 12.9"

# Seuil test individuel (plus strict)
threshold_test: 0.60  # vs 0.70 default

# Seuils SURVIVES (plus difficile à atteindre)
survives:
  score_max: 0.30       # vs 0.40 default
  robustness_min: 0.70  # vs 0.60 default
  majority_min: 0.60    # vs 0.50 default

# Seuils REJECTED (plus facile à atteindre)
rejected:
  score_min: 0.70       # vs 0.60 default
  robustness_max: 0.30  # vs 0.40 default
  majority_max: 0.15    # vs 0.20 default