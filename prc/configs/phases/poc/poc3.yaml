# configs/phases/poc/poc2b.yaml
# Run POC2b — poc2 + seed axis pour validation cohérence et calibration thresholds
# 224 compositions × 15 seeds = ~3360 runs
# Objectif : base statistiquement robuste pour calibration YAML + clustering gamma-driven

phase: poc3
max_iterations: 1,5,10,15,20,25,50
n_dof: 100
seed_CI: 0,1,2,3,15,26,42,92,101,201,304,458,561,672,791,837
seed_run: 0,1,2,3,15,26,42,92,101,201,304,458,561,672,791,837
axes:
  gamma: all
  encoding: all
  modifier: all

