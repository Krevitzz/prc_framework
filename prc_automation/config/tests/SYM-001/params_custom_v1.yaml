# config/tests/SYM-001/params_custom_v1.yaml

version: "1.0"
config_id: "custom_v1"
description: "Configuration stricte pour SYM-001"

# Override params
asymmetry_norm:
  norm_type: 'spectral'  # Change type norme
  post_process: 'log'    # Change post-processing

asymmetry_norm_normalized:
  epsilon: 1e-8  # Plus strict